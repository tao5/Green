<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".mvp.view.home.impl.MainActivity">

    <include
        android:id="@+id/drawer_content_head_collapsing_"
        layout="@layout/drawer_content_head_collapsing"
        android:visibility="visible"/>

    <include
        android:id="@+id/drawer_content_head_normal_"
        layout="@layout/drawer_content_head_normal"
        android:visibility="gone"/>

    <include layout="@layout/drawer_content_main" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/drawer_content_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:src="@android:drawable/ic_dialog_email" />

</android.support.design.widget.CoordinatorLayout>
